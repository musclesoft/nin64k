MEMORY {
    LOADADDR: start = $07FF, size = $0002;
    MAIN:     start = $0801, size = $F7FE;
}

SEGMENTS {
    LOADADDR: load = LOADADDR;
    CODE:     load = MAIN;
    PART1:    load = MAIN, start = $2000, optional = yes;
    STREAM:   load = MAIN, start = $6000, optional = yes;
}
